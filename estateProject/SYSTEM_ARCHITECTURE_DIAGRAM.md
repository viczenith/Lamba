## 🏗️ SYSTEM ARCHITECTURE - UNIQUE ID & DATA ISOLATION

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          NEW USER REGISTRATION                              │
└─────────────────────────────────────────────────────────────────────────────┘

    User submits registration form
            │
            ▼
    ┌──────────────────────────┐
    │  MarketerUser/ClientUser │
    │   instance created       │
    └──────────────────────────┘
            │
            ▼
    ┌──────────────────────────┐
    │   .save() method called  │
    └──────────────────────────┘
            │
            ▼ (if company_marketer_id missing)
    ┌──────────────────────────────────────────┐
    │ CompanySequence.get_next()               │
    │ - Database lock (select_for_update)      │
    │ - Atomic increment: last_value += 1      │
    │ - Return next ID (e.g., 3)               │
    └──────────────────────────────────────────┘
            │ Atomic | No duplicates possible
            ▼
    ┌──────────────────────────────────────────┐
    │ Generate UID                             │
    │ - Get company prefix: "LPL"              │
    │ - Format: "{prefix}MKT{id:03d}"         │
    │ - Result: "LPL-MKT003"                   │
    │ - Check for duplicates (never happens)   │
    └──────────────────────────────────────────┘
            │
            ▼
    ┌──────────────────────────────────────────┐
    │ Database UNIQUE Constraint Check         │
    │ - company_marketer_uid UNIQUE            │
    │ - Rejects any duplicates                 │
    └──────────────────────────────────────────┘
            │
            ▼
    ┌──────────────────────────┐
    │   User created with ID   │
    │ Immediately visible in   │
    │  listings with unique UID│
    └──────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                      DATA ISOLATION - 5 LAYERS                              │
└─────────────────────────────────────────────────────────────────────────────┘

LAYER 1: MIDDLEWARE
┌────────────────────────────────────────┐
│ get_current_company() from request     │
│ Stores company in thread-local storage │
└────────────────────────────────────────┘
         │
         ▼
LAYER 2: QUERYSETS - CompanyAwareManager
┌────────────────────────────────────────┐
│ Estate.objects                         │
│ PlotSize.objects                       │
│ PlotNumber.objects                     │
│ ↓                                      │
│ Auto-filtered by company_profile       │
└────────────────────────────────────────┘
         │
         ▼
LAYER 3: ORM - Foreign Keys with Company
┌────────────────────────────────────────┐
│ ClientMarketerAssignment.company       │
│ Message.company                        │
│ ↓                                      │
│ Foreign keys enforce company scoping   │
└────────────────────────────────────────┘
         │
         ▼
LAYER 4: VIEWS - Explicit Filtering
┌────────────────────────────────────────┐
│ marketer_list:                         │
│   .filter(company_profile=company)     │
│                                        │
│ client:                                │
│   .filter(company_profile=company)     │
└────────────────────────────────────────┘
         │
         ▼
LAYER 5: DATABASE - UNIQUE Constraints
┌────────────────────────────────────────┐
│ company_marketer_uid UNIQUE            │
│ company_client_uid UNIQUE              │
│ unique_together = ('client', 'company')│
│ ↓                                      │
│ Rejects duplicate/cross-company data   │
└────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                    UID FORMAT & UNIQUENESS STRUCTURE                        │
└─────────────────────────────────────────────────────────────────────────────┘

Company: Lamba Property Limited
Prefix: LPL (from company name)

MARKETER IDs:
┌─────────────────────────────────────────────────────────────────────┐
│ Format: {PREFIX}-{ROLE}{SEQUENCE}                                  │
├─────────────────────────────────────────────────────────────────────┤
│ UID            │ Company Seq │ User                                 │
├─────────────────────────────────────────────────────────────────────┤
│ LPL-MKT001    │ 1           │ akorvikkyy@gmail.com                │
│ LPL-MKT002    │ 2           │ victorgodwinakor@gmail.com          │
│ LPL-MKT003    │ 3           │ new_marketer@example.com (auto)     │
│ ...           │ ...         │ ...                                  │
└─────────────────────────────────────────────────────────────────────┘

CLIENT IDs:
┌─────────────────────────────────────────────────────────────────────┐
│ Format: {PREFIX}-{ROLE}{SEQUENCE}                                  │
├─────────────────────────────────────────────────────────────────────┤
│ UID            │ Company Seq │ User                                 │
├─────────────────────────────────────────────────────────────────────┤
│ LPL-CLT001    │ 1           │ akorvikkyy@gmail.com                │
│ LPL-CLT002    │ 2           │ (skipped - deleted user)            │
│ LPL-CLT003    │ 3           │ (skipped - deleted user)            │
│ LPL-CLT004    │ 4           │ victorgodwinakor@gmail.com          │
│ LPL-CLT005    │ 5           │ new_client@example.com (auto)       │
│ ...           │ ...         │ ...                                  │
└─────────────────────────────────────────────────────────────────────┘

Each company has independent sequences:
┌──────────────────────────────────────────────────────────┐
│ Company: Test Company                                    │
│ Prefix: TST                                              │
├──────────────────────────────────────────────────────────┤
│ TST-MKT001, TST-MKT002, TST-CLT001, TST-CLT002, etc.    │
└──────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                    VERIFICATION & MONITORING                                │
└─────────────────────────────────────────────────────────────────────────────┘

Daily/Weekly Checks:
┌────────────────────────────────────────┐
│ python security_audit.py               │
│ python final_verification_report.py    │
│ python verify_all_uids.py              │
│                                        │
│ Expected: ✅ ALL CHECKS PASSED         │
└────────────────────────────────────────┘

Database Queries to Monitor:
┌────────────────────────────────────────────────────────────────┐
│ -- Check for duplicates                                        │
│ SELECT company_marketer_uid, COUNT(*)                         │
│ FROM estateApp_marketeruser                                   │
│ GROUP BY company_marketer_uid HAVING COUNT(*) > 1;            │
│                                                               │
│ -- Check sequence state                                       │
│ SELECT company_id, key, last_value                            │
│ FROM estateApp_companysequence;                               │
└────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                      CURRENT SYSTEM STATE                                   │
└─────────────────────────────────────────────────────────────────────────────┘

                            ✅ PRODUCTION READY

✓ No duplicate UIDs anywhere in system
✓ New users get unique IDs automatically
✓ All data isolation layers active
✓ Database constraints prevent bypasses
✓ Atomic sequences prevent race conditions
✓ Format consistent: PREFIX-ROLE### format

Next actions: NONE (system working correctly)
